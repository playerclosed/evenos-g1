{
  "name": "evenos-g1",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["packages/*", "runtime/*/*", "apps/*", "server/*/*"],
  "scripts": {
    "build": "pnpm -r --parallel build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}